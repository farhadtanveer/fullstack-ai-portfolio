# Copy to .env — never commit the real file
APP_ENV=development

# Database
POSTGRES_USER=admin
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=industrial_dashboard

# Auth
SECRET_KEY=replace-with-64-char-random-string

# Frontend
VITE_API_URL=http://localhost:8000
